# One server = one URL. If you change PORT, use that URL in the browser.
PORT=3040

# Optional: only if you serve the UI from a different origin
# CORS_ORIGIN=http://localhost:3040

# How many ports to try if PORT is busy (no spam per hop; only final line prints)
PORT_TRY_COUNT=15
